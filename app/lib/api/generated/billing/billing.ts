/**
 * Generated by orval v8.8.1 🍺
 * Do not edit manually.
 * Kippo Project Management API
 * REST API for managing Kippo projects, tasks, and effort tracking
 * OpenAPI spec version: 1.1.0
 */
import type {
  BillingListEntry,
  BillingListParams,
  BillingRetrieveParams,
  PaginatedBillingListEntryList
} from '../models';

import { customFetch } from '../../custom-fetch';

/**
 * Flat, cross-project billing ledger for the 請求一覧 (billing list) UI — read-only.

One row per ``KippoProjectBillingEntry`` across the request user's accessible projects
(superusers see all), denormalized with its project / contract / customer display fields via
``BillingListEntrySerializer``. Org-scoped through the contract's project organization. Supports
``month`` / ``from`` / ``to`` / ``project`` server-side filtering to bound the window; the UI does
its own free-text search, 完了 toggle and summation client-side over the returned rows.
 */
export type billingListResponse200 = {
  data: PaginatedBillingListEntryList
  status: 200
}

export type billingListResponseSuccess = (billingListResponse200) & {
  headers: Headers;
};
;

export type billingListResponse = (billingListResponseSuccess)

export const getBillingListUrl = (params?: BillingListParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/billing/?${stringifiedParams}` : `/api/billing/`
}

export const billingList = async (params?: BillingListParams, options?: RequestInit): Promise<billingListResponse> => {

  return customFetch<billingListResponse>(getBillingListUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


/**
 * Flat, cross-project billing ledger for the 請求一覧 (billing list) UI — read-only.

One row per ``KippoProjectBillingEntry`` across the request user's accessible projects
(superusers see all), denormalized with its project / contract / customer display fields via
``BillingListEntrySerializer``. Org-scoped through the contract's project organization. Supports
``month`` / ``from`` / ``to`` / ``project`` server-side filtering to bound the window; the UI does
its own free-text search, 完了 toggle and summation client-side over the returned rows.
 */
export type billingRetrieveResponse200 = {
  data: BillingListEntry
  status: 200
}

export type billingRetrieveResponseSuccess = (billingRetrieveResponse200) & {
  headers: Headers;
};
;

export type billingRetrieveResponse = (billingRetrieveResponseSuccess)

export const getBillingRetrieveUrl = (id: number,
    params?: BillingRetrieveParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/billing/${id}/?${stringifiedParams}` : `/api/billing/${id}/`
}

export const billingRetrieve = async (id: number,
    params?: BillingRetrieveParams, options?: RequestInit): Promise<billingRetrieveResponse> => {

  return customFetch<billingRetrieveResponse>(getBillingRetrieveUrl(id,params),
  {
    ...options,
    method: 'GET'


  }
);}


