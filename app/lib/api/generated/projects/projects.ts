/**
 * Generated by orval v7.17.2 🍺
 * Do not edit manually.
 * Kippo Project Management API
 * REST API for managing Kippo projects, tasks, and effort tracking
 * OpenAPI spec version: 1.1.0
 */
import type {
  KippoProject,
  KippoProjectRequest,
  PaginatedKippoProjectList,
  PaginatedProjectWeeklyEffortList,
  PatchedKippoProjectRequest,
  PatchedProjectWeeklyEffortRequest,
  ProjectWeeklyEffort,
  ProjectWeeklyEffortRequest,
  ProjectsListParams,
  ProjectsWeeklyeffortListParams
} from '.././models';

import { customFetch } from '../../custom-fetch';

/**
 * ViewSet for KippoProject model.

**Organization Scoping:**
- Regular users can only access projects from organizations they belong to
- If a user belongs to multiple organizations, they can access projects from ALL of them
- Superusers can access projects from all organizations

**Filtering:**
- is_active: Filter by display_as_active field (true/false)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsListResponse200 = {
  data: PaginatedKippoProjectList
  status: 200
}
    
export type projectsListResponseSuccess = (projectsListResponse200) & {
  headers: Headers;
};
;

export type projectsListResponse = (projectsListResponseSuccess)

export const getProjectsListUrl = (params?: ProjectsListParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/projects/?${stringifiedParams}` : `/api/projects/`
}

export const projectsList = async (params?: ProjectsListParams, options?: RequestInit): Promise<projectsListResponse> => {
  
  return customFetch<projectsListResponse>(getProjectsListUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}


/**
 * ViewSet for KippoProject model.

**Organization Scoping:**
- Regular users can only access projects from organizations they belong to
- If a user belongs to multiple organizations, they can access projects from ALL of them
- Superusers can access projects from all organizations

**Filtering:**
- is_active: Filter by display_as_active field (true/false)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsCreateResponse201 = {
  data: KippoProject
  status: 201
}
    
export type projectsCreateResponseSuccess = (projectsCreateResponse201) & {
  headers: Headers;
};
;

export type projectsCreateResponse = (projectsCreateResponseSuccess)

export const getProjectsCreateUrl = () => {


  

  return `/api/projects/`
}

export const projectsCreate = async (kippoProjectRequest: KippoProjectRequest, options?: RequestInit): Promise<projectsCreateResponse> => {
  
  return customFetch<projectsCreateResponse>(getProjectsCreateUrl(),
  {      
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      kippoProjectRequest,)
  }
);}


/**
 * ViewSet for KippoProject model.

**Organization Scoping:**
- Regular users can only access projects from organizations they belong to
- If a user belongs to multiple organizations, they can access projects from ALL of them
- Superusers can access projects from all organizations

**Filtering:**
- is_active: Filter by display_as_active field (true/false)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsRetrieveResponse200 = {
  data: KippoProject
  status: 200
}
    
export type projectsRetrieveResponseSuccess = (projectsRetrieveResponse200) & {
  headers: Headers;
};
;

export type projectsRetrieveResponse = (projectsRetrieveResponseSuccess)

export const getProjectsRetrieveUrl = (id: string,) => {


  

  return `/api/projects/${id}/`
}

export const projectsRetrieve = async (id: string, options?: RequestInit): Promise<projectsRetrieveResponse> => {
  
  return customFetch<projectsRetrieveResponse>(getProjectsRetrieveUrl(id),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}


/**
 * ViewSet for KippoProject model.

**Organization Scoping:**
- Regular users can only access projects from organizations they belong to
- If a user belongs to multiple organizations, they can access projects from ALL of them
- Superusers can access projects from all organizations

**Filtering:**
- is_active: Filter by display_as_active field (true/false)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsUpdateResponse200 = {
  data: KippoProject
  status: 200
}
    
export type projectsUpdateResponseSuccess = (projectsUpdateResponse200) & {
  headers: Headers;
};
;

export type projectsUpdateResponse = (projectsUpdateResponseSuccess)

export const getProjectsUpdateUrl = (id: string,) => {


  

  return `/api/projects/${id}/`
}

export const projectsUpdate = async (id: string,
    kippoProjectRequest: KippoProjectRequest, options?: RequestInit): Promise<projectsUpdateResponse> => {
  
  return customFetch<projectsUpdateResponse>(getProjectsUpdateUrl(id),
  {      
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      kippoProjectRequest,)
  }
);}


/**
 * ViewSet for KippoProject model.

**Organization Scoping:**
- Regular users can only access projects from organizations they belong to
- If a user belongs to multiple organizations, they can access projects from ALL of them
- Superusers can access projects from all organizations

**Filtering:**
- is_active: Filter by display_as_active field (true/false)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsPartialUpdateResponse200 = {
  data: KippoProject
  status: 200
}
    
export type projectsPartialUpdateResponseSuccess = (projectsPartialUpdateResponse200) & {
  headers: Headers;
};
;

export type projectsPartialUpdateResponse = (projectsPartialUpdateResponseSuccess)

export const getProjectsPartialUpdateUrl = (id: string,) => {


  

  return `/api/projects/${id}/`
}

export const projectsPartialUpdate = async (id: string,
    patchedKippoProjectRequest: PatchedKippoProjectRequest, options?: RequestInit): Promise<projectsPartialUpdateResponse> => {
  
  return customFetch<projectsPartialUpdateResponse>(getProjectsPartialUpdateUrl(id),
  {      
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      patchedKippoProjectRequest,)
  }
);}


/**
 * ViewSet for KippoProject model.

**Organization Scoping:**
- Regular users can only access projects from organizations they belong to
- If a user belongs to multiple organizations, they can access projects from ALL of them
- Superusers can access projects from all organizations

**Filtering:**
- is_active: Filter by display_as_active field (true/false)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsDestroyResponse204 = {
  data: void
  status: 204
}
    
export type projectsDestroyResponseSuccess = (projectsDestroyResponse204) & {
  headers: Headers;
};
;

export type projectsDestroyResponse = (projectsDestroyResponseSuccess)

export const getProjectsDestroyUrl = (id: string,) => {


  

  return `/api/projects/${id}/`
}

export const projectsDestroy = async (id: string, options?: RequestInit): Promise<projectsDestroyResponse> => {
  
  return customFetch<projectsDestroyResponse>(getProjectsDestroyUrl(id),
  {      
    ...options,
    method: 'DELETE'
    
    
  }
);}


/**
 * ViewSet for ProjectWeeklyEffort model.

**Organization Scoping:**
- Regular users can only access weekly effort for projects in organizations they belong to
- If a user belongs to multiple organizations, they can access efforts from ALL of them
- Superusers can access weekly effort entries from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- week_start_gte: Filter by week_start greater than or equal to date (YYYY-MM-DD)
- week_start_lte: Filter by week_start less than or equal to date (YYYY-MM-DD)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsWeeklyeffortListResponse200 = {
  data: PaginatedProjectWeeklyEffortList
  status: 200
}
    
export type projectsWeeklyeffortListResponseSuccess = (projectsWeeklyeffortListResponse200) & {
  headers: Headers;
};
;

export type projectsWeeklyeffortListResponse = (projectsWeeklyeffortListResponseSuccess)

export const getProjectsWeeklyeffortListUrl = (params?: ProjectsWeeklyeffortListParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/projects/weeklyeffort/?${stringifiedParams}` : `/api/projects/weeklyeffort/`
}

export const projectsWeeklyeffortList = async (params?: ProjectsWeeklyeffortListParams, options?: RequestInit): Promise<projectsWeeklyeffortListResponse> => {
  
  return customFetch<projectsWeeklyeffortListResponse>(getProjectsWeeklyeffortListUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}


/**
 * ViewSet for ProjectWeeklyEffort model.

**Organization Scoping:**
- Regular users can only access weekly effort for projects in organizations they belong to
- If a user belongs to multiple organizations, they can access efforts from ALL of them
- Superusers can access weekly effort entries from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- week_start_gte: Filter by week_start greater than or equal to date (YYYY-MM-DD)
- week_start_lte: Filter by week_start less than or equal to date (YYYY-MM-DD)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsWeeklyeffortCreateResponse201 = {
  data: ProjectWeeklyEffort
  status: 201
}
    
export type projectsWeeklyeffortCreateResponseSuccess = (projectsWeeklyeffortCreateResponse201) & {
  headers: Headers;
};
;

export type projectsWeeklyeffortCreateResponse = (projectsWeeklyeffortCreateResponseSuccess)

export const getProjectsWeeklyeffortCreateUrl = () => {


  

  return `/api/projects/weeklyeffort/`
}

export const projectsWeeklyeffortCreate = async (projectWeeklyEffortRequest: ProjectWeeklyEffortRequest, options?: RequestInit): Promise<projectsWeeklyeffortCreateResponse> => {
  
  return customFetch<projectsWeeklyeffortCreateResponse>(getProjectsWeeklyeffortCreateUrl(),
  {      
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      projectWeeklyEffortRequest,)
  }
);}


/**
 * ViewSet for ProjectWeeklyEffort model.

**Organization Scoping:**
- Regular users can only access weekly effort for projects in organizations they belong to
- If a user belongs to multiple organizations, they can access efforts from ALL of them
- Superusers can access weekly effort entries from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- week_start_gte: Filter by week_start greater than or equal to date (YYYY-MM-DD)
- week_start_lte: Filter by week_start less than or equal to date (YYYY-MM-DD)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsWeeklyeffortRetrieveResponse200 = {
  data: ProjectWeeklyEffort
  status: 200
}
    
export type projectsWeeklyeffortRetrieveResponseSuccess = (projectsWeeklyeffortRetrieveResponse200) & {
  headers: Headers;
};
;

export type projectsWeeklyeffortRetrieveResponse = (projectsWeeklyeffortRetrieveResponseSuccess)

export const getProjectsWeeklyeffortRetrieveUrl = (id: number,) => {


  

  return `/api/projects/weeklyeffort/${id}/`
}

export const projectsWeeklyeffortRetrieve = async (id: number, options?: RequestInit): Promise<projectsWeeklyeffortRetrieveResponse> => {
  
  return customFetch<projectsWeeklyeffortRetrieveResponse>(getProjectsWeeklyeffortRetrieveUrl(id),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}


/**
 * ViewSet for ProjectWeeklyEffort model.

**Organization Scoping:**
- Regular users can only access weekly effort for projects in organizations they belong to
- If a user belongs to multiple organizations, they can access efforts from ALL of them
- Superusers can access weekly effort entries from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- week_start_gte: Filter by week_start greater than or equal to date (YYYY-MM-DD)
- week_start_lte: Filter by week_start less than or equal to date (YYYY-MM-DD)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsWeeklyeffortUpdateResponse200 = {
  data: ProjectWeeklyEffort
  status: 200
}
    
export type projectsWeeklyeffortUpdateResponseSuccess = (projectsWeeklyeffortUpdateResponse200) & {
  headers: Headers;
};
;

export type projectsWeeklyeffortUpdateResponse = (projectsWeeklyeffortUpdateResponseSuccess)

export const getProjectsWeeklyeffortUpdateUrl = (id: number,) => {


  

  return `/api/projects/weeklyeffort/${id}/`
}

export const projectsWeeklyeffortUpdate = async (id: number,
    projectWeeklyEffortRequest: ProjectWeeklyEffortRequest, options?: RequestInit): Promise<projectsWeeklyeffortUpdateResponse> => {
  
  return customFetch<projectsWeeklyeffortUpdateResponse>(getProjectsWeeklyeffortUpdateUrl(id),
  {      
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      projectWeeklyEffortRequest,)
  }
);}


/**
 * ViewSet for ProjectWeeklyEffort model.

**Organization Scoping:**
- Regular users can only access weekly effort for projects in organizations they belong to
- If a user belongs to multiple organizations, they can access efforts from ALL of them
- Superusers can access weekly effort entries from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- week_start_gte: Filter by week_start greater than or equal to date (YYYY-MM-DD)
- week_start_lte: Filter by week_start less than or equal to date (YYYY-MM-DD)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsWeeklyeffortPartialUpdateResponse200 = {
  data: ProjectWeeklyEffort
  status: 200
}
    
export type projectsWeeklyeffortPartialUpdateResponseSuccess = (projectsWeeklyeffortPartialUpdateResponse200) & {
  headers: Headers;
};
;

export type projectsWeeklyeffortPartialUpdateResponse = (projectsWeeklyeffortPartialUpdateResponseSuccess)

export const getProjectsWeeklyeffortPartialUpdateUrl = (id: number,) => {


  

  return `/api/projects/weeklyeffort/${id}/`
}

export const projectsWeeklyeffortPartialUpdate = async (id: number,
    patchedProjectWeeklyEffortRequest: PatchedProjectWeeklyEffortRequest, options?: RequestInit): Promise<projectsWeeklyeffortPartialUpdateResponse> => {
  
  return customFetch<projectsWeeklyeffortPartialUpdateResponse>(getProjectsWeeklyeffortPartialUpdateUrl(id),
  {      
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      patchedProjectWeeklyEffortRequest,)
  }
);}


/**
 * ViewSet for ProjectWeeklyEffort model.

**Organization Scoping:**
- Regular users can only access weekly effort for projects in organizations they belong to
- If a user belongs to multiple organizations, they can access efforts from ALL of them
- Superusers can access weekly effort entries from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- week_start_gte: Filter by week_start greater than or equal to date (YYYY-MM-DD)
- week_start_lte: Filter by week_start less than or equal to date (YYYY-MM-DD)

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users)
- Update (PUT/PATCH): Authenticated users (organization-scoped for regular users)
- Create (POST): Superusers only
- Delete (DELETE): Superusers only
 */
export type projectsWeeklyeffortDestroyResponse204 = {
  data: void
  status: 204
}
    
export type projectsWeeklyeffortDestroyResponseSuccess = (projectsWeeklyeffortDestroyResponse204) & {
  headers: Headers;
};
;

export type projectsWeeklyeffortDestroyResponse = (projectsWeeklyeffortDestroyResponseSuccess)

export const getProjectsWeeklyeffortDestroyUrl = (id: number,) => {


  

  return `/api/projects/weeklyeffort/${id}/`
}

export const projectsWeeklyeffortDestroy = async (id: number, options?: RequestInit): Promise<projectsWeeklyeffortDestroyResponse> => {
  
  return customFetch<projectsWeeklyeffortDestroyResponse>(getProjectsWeeklyeffortDestroyUrl(id),
  {      
    ...options,
    method: 'DELETE'
    
    
  }
);}


