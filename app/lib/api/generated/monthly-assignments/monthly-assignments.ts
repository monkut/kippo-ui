/**
 * Generated by orval v8.0.1 🍺
 * Do not edit manually.
 * Kippo Project Management API
 * REST API for managing Kippo projects, tasks, and effort tracking
 * OpenAPI spec version: 1.1.0
 */
import type {
  MonthlyAssignmentsListParams,
  PaginatedProjectMonthlyAssignmentList,
  PatchedProjectMonthlyAssignmentRequest,
  ProjectMonthlyAssignment,
  ProjectMonthlyAssignmentRequest
} from '.././models';

import { customFetch } from '../../custom-fetch';

/**
 * ViewSet for ProjectMonthlyAssignment model.

Manages monthly workload percentage assignments for users on projects.

**Organization Scoping:**
- Regular users can only access assignments for projects in organizations they belong to
- Superusers can access assignments from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- month: Filter by exact month (YYYY-MM-DD format, day should be 01)
- month_gte: Filter by month >= date (YYYY-MM-DD format)
- month_lte: Filter by month <= date (YYYY-MM-DD format)

**Validation:**
- User must be a member of the project's organization
- Month defaults to the project's start_date month if not provided
- A warning is logged if total assignment percentage for a user exceeds 100% in an organization

**Permissions:**
- All CRUD operations require authentication
- Users can only manage assignments for projects in their organizations
 */
export type monthlyAssignmentsListResponse200 = {
  data: PaginatedProjectMonthlyAssignmentList
  status: 200
}
    
export type monthlyAssignmentsListResponseSuccess = (monthlyAssignmentsListResponse200) & {
  headers: Headers;
};
;

export type monthlyAssignmentsListResponse = (monthlyAssignmentsListResponseSuccess)

export const getMonthlyAssignmentsListUrl = (params?: MonthlyAssignmentsListParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/monthly-assignments/?${stringifiedParams}` : `/api/monthly-assignments/`
}

export const monthlyAssignmentsList = async (params?: MonthlyAssignmentsListParams, options?: RequestInit): Promise<monthlyAssignmentsListResponse> => {
  
  return customFetch<monthlyAssignmentsListResponse>(getMonthlyAssignmentsListUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}


/**
 * ViewSet for ProjectMonthlyAssignment model.

Manages monthly workload percentage assignments for users on projects.

**Organization Scoping:**
- Regular users can only access assignments for projects in organizations they belong to
- Superusers can access assignments from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- month: Filter by exact month (YYYY-MM-DD format, day should be 01)
- month_gte: Filter by month >= date (YYYY-MM-DD format)
- month_lte: Filter by month <= date (YYYY-MM-DD format)

**Validation:**
- User must be a member of the project's organization
- Month defaults to the project's start_date month if not provided
- A warning is logged if total assignment percentage for a user exceeds 100% in an organization

**Permissions:**
- All CRUD operations require authentication
- Users can only manage assignments for projects in their organizations
 */
export type monthlyAssignmentsCreateResponse201 = {
  data: ProjectMonthlyAssignment
  status: 201
}
    
export type monthlyAssignmentsCreateResponseSuccess = (monthlyAssignmentsCreateResponse201) & {
  headers: Headers;
};
;

export type monthlyAssignmentsCreateResponse = (monthlyAssignmentsCreateResponseSuccess)

export const getMonthlyAssignmentsCreateUrl = () => {


  

  return `/api/monthly-assignments/`
}

export const monthlyAssignmentsCreate = async (projectMonthlyAssignmentRequest: ProjectMonthlyAssignmentRequest, options?: RequestInit): Promise<monthlyAssignmentsCreateResponse> => {
  
  return customFetch<monthlyAssignmentsCreateResponse>(getMonthlyAssignmentsCreateUrl(),
  {      
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      projectMonthlyAssignmentRequest,)
  }
);}


/**
 * ViewSet for ProjectMonthlyAssignment model.

Manages monthly workload percentage assignments for users on projects.

**Organization Scoping:**
- Regular users can only access assignments for projects in organizations they belong to
- Superusers can access assignments from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- month: Filter by exact month (YYYY-MM-DD format, day should be 01)
- month_gte: Filter by month >= date (YYYY-MM-DD format)
- month_lte: Filter by month <= date (YYYY-MM-DD format)

**Validation:**
- User must be a member of the project's organization
- Month defaults to the project's start_date month if not provided
- A warning is logged if total assignment percentage for a user exceeds 100% in an organization

**Permissions:**
- All CRUD operations require authentication
- Users can only manage assignments for projects in their organizations
 */
export type monthlyAssignmentsRetrieveResponse200 = {
  data: ProjectMonthlyAssignment
  status: 200
}
    
export type monthlyAssignmentsRetrieveResponseSuccess = (monthlyAssignmentsRetrieveResponse200) & {
  headers: Headers;
};
;

export type monthlyAssignmentsRetrieveResponse = (monthlyAssignmentsRetrieveResponseSuccess)

export const getMonthlyAssignmentsRetrieveUrl = (id: number,) => {


  

  return `/api/monthly-assignments/${id}/`
}

export const monthlyAssignmentsRetrieve = async (id: number, options?: RequestInit): Promise<monthlyAssignmentsRetrieveResponse> => {
  
  return customFetch<monthlyAssignmentsRetrieveResponse>(getMonthlyAssignmentsRetrieveUrl(id),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}


/**
 * ViewSet for ProjectMonthlyAssignment model.

Manages monthly workload percentage assignments for users on projects.

**Organization Scoping:**
- Regular users can only access assignments for projects in organizations they belong to
- Superusers can access assignments from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- month: Filter by exact month (YYYY-MM-DD format, day should be 01)
- month_gte: Filter by month >= date (YYYY-MM-DD format)
- month_lte: Filter by month <= date (YYYY-MM-DD format)

**Validation:**
- User must be a member of the project's organization
- Month defaults to the project's start_date month if not provided
- A warning is logged if total assignment percentage for a user exceeds 100% in an organization

**Permissions:**
- All CRUD operations require authentication
- Users can only manage assignments for projects in their organizations
 */
export type monthlyAssignmentsUpdateResponse200 = {
  data: ProjectMonthlyAssignment
  status: 200
}
    
export type monthlyAssignmentsUpdateResponseSuccess = (monthlyAssignmentsUpdateResponse200) & {
  headers: Headers;
};
;

export type monthlyAssignmentsUpdateResponse = (monthlyAssignmentsUpdateResponseSuccess)

export const getMonthlyAssignmentsUpdateUrl = (id: number,) => {


  

  return `/api/monthly-assignments/${id}/`
}

export const monthlyAssignmentsUpdate = async (id: number,
    projectMonthlyAssignmentRequest: ProjectMonthlyAssignmentRequest, options?: RequestInit): Promise<monthlyAssignmentsUpdateResponse> => {
  
  return customFetch<monthlyAssignmentsUpdateResponse>(getMonthlyAssignmentsUpdateUrl(id),
  {      
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      projectMonthlyAssignmentRequest,)
  }
);}


/**
 * ViewSet for ProjectMonthlyAssignment model.

Manages monthly workload percentage assignments for users on projects.

**Organization Scoping:**
- Regular users can only access assignments for projects in organizations they belong to
- Superusers can access assignments from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- month: Filter by exact month (YYYY-MM-DD format, day should be 01)
- month_gte: Filter by month >= date (YYYY-MM-DD format)
- month_lte: Filter by month <= date (YYYY-MM-DD format)

**Validation:**
- User must be a member of the project's organization
- Month defaults to the project's start_date month if not provided
- A warning is logged if total assignment percentage for a user exceeds 100% in an organization

**Permissions:**
- All CRUD operations require authentication
- Users can only manage assignments for projects in their organizations
 */
export type monthlyAssignmentsPartialUpdateResponse200 = {
  data: ProjectMonthlyAssignment
  status: 200
}
    
export type monthlyAssignmentsPartialUpdateResponseSuccess = (monthlyAssignmentsPartialUpdateResponse200) & {
  headers: Headers;
};
;

export type monthlyAssignmentsPartialUpdateResponse = (monthlyAssignmentsPartialUpdateResponseSuccess)

export const getMonthlyAssignmentsPartialUpdateUrl = (id: number,) => {


  

  return `/api/monthly-assignments/${id}/`
}

export const monthlyAssignmentsPartialUpdate = async (id: number,
    patchedProjectMonthlyAssignmentRequest: PatchedProjectMonthlyAssignmentRequest, options?: RequestInit): Promise<monthlyAssignmentsPartialUpdateResponse> => {
  
  return customFetch<monthlyAssignmentsPartialUpdateResponse>(getMonthlyAssignmentsPartialUpdateUrl(id),
  {      
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      patchedProjectMonthlyAssignmentRequest,)
  }
);}


/**
 * ViewSet for ProjectMonthlyAssignment model.

Manages monthly workload percentage assignments for users on projects.

**Organization Scoping:**
- Regular users can only access assignments for projects in organizations they belong to
- Superusers can access assignments from all organizations

**Filtering:**
- project: Filter by project UUID
- user: Filter by user ID
- month: Filter by exact month (YYYY-MM-DD format, day should be 01)
- month_gte: Filter by month >= date (YYYY-MM-DD format)
- month_lte: Filter by month <= date (YYYY-MM-DD format)

**Validation:**
- User must be a member of the project's organization
- Month defaults to the project's start_date month if not provided
- A warning is logged if total assignment percentage for a user exceeds 100% in an organization

**Permissions:**
- All CRUD operations require authentication
- Users can only manage assignments for projects in their organizations
 */
export type monthlyAssignmentsDestroyResponse204 = {
  data: void
  status: 204
}
    
export type monthlyAssignmentsDestroyResponseSuccess = (monthlyAssignmentsDestroyResponse204) & {
  headers: Headers;
};
;

export type monthlyAssignmentsDestroyResponse = (monthlyAssignmentsDestroyResponseSuccess)

export const getMonthlyAssignmentsDestroyUrl = (id: number,) => {


  

  return `/api/monthly-assignments/${id}/`
}

export const monthlyAssignmentsDestroy = async (id: number, options?: RequestInit): Promise<monthlyAssignmentsDestroyResponse> => {
  
  return customFetch<monthlyAssignmentsDestroyResponse>(getMonthlyAssignmentsDestroyUrl(id),
  {      
    ...options,
    method: 'DELETE'
    
    
  }
);}


