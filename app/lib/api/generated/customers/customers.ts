/**
 * Generated by orval v8.8.1 🍺
 * Do not edit manually.
 * Kippo Project Management API
 * REST API for managing Kippo projects, tasks, and effort tracking
 * OpenAPI spec version: 1.1.0
 */
import type {
  CustomersListParams,
  KippoCustomer,
  KippoCustomerRequest,
  PaginatedKippoCustomerList,
  PatchedKippoCustomerRequest
} from '../models';

import { customFetch } from '../../custom-fetch';

/**
 * ViewSet for KippoCustomer model.

**Organization Scoping:**
- Regular users see only customers from organizations they belong to.
- Superusers see all customers.

**Filtering:**
- is_active: Filter by display_as_active (true/false).
- organization: UUID filter (still intersected with user's memberships).
- search: SearchFilter on `name`, `email`.

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users).
- Create (POST): Authenticated users (organization-membership enforced by serializer).
- Update (PUT/PATCH): Authenticated users (organization-scoped).
- Delete (DELETE): Superusers only.
 */
export type customersListResponse200 = {
  data: PaginatedKippoCustomerList
  status: 200
}

export type customersListResponseSuccess = (customersListResponse200) & {
  headers: Headers;
};
;

export type customersListResponse = (customersListResponseSuccess)

export const getCustomersListUrl = (params?: CustomersListParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/customers/?${stringifiedParams}` : `/api/customers/`
}

export const customersList = async (params?: CustomersListParams, options?: RequestInit): Promise<customersListResponse> => {

  return customFetch<customersListResponse>(getCustomersListUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


/**
 * ViewSet for KippoCustomer model.

**Organization Scoping:**
- Regular users see only customers from organizations they belong to.
- Superusers see all customers.

**Filtering:**
- is_active: Filter by display_as_active (true/false).
- organization: UUID filter (still intersected with user's memberships).
- search: SearchFilter on `name`, `email`.

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users).
- Create (POST): Authenticated users (organization-membership enforced by serializer).
- Update (PUT/PATCH): Authenticated users (organization-scoped).
- Delete (DELETE): Superusers only.
 */
export type customersCreateResponse201 = {
  data: KippoCustomer
  status: 201
}

export type customersCreateResponseSuccess = (customersCreateResponse201) & {
  headers: Headers;
};
;

export type customersCreateResponse = (customersCreateResponseSuccess)

export const getCustomersCreateUrl = () => {




  return `/api/customers/`
}

export const customersCreate = async (kippoCustomerRequest: KippoCustomerRequest, options?: RequestInit): Promise<customersCreateResponse> => {

  return customFetch<customersCreateResponse>(getCustomersCreateUrl(),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      kippoCustomerRequest,)
  }
);}


/**
 * ViewSet for KippoCustomer model.

**Organization Scoping:**
- Regular users see only customers from organizations they belong to.
- Superusers see all customers.

**Filtering:**
- is_active: Filter by display_as_active (true/false).
- organization: UUID filter (still intersected with user's memberships).
- search: SearchFilter on `name`, `email`.

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users).
- Create (POST): Authenticated users (organization-membership enforced by serializer).
- Update (PUT/PATCH): Authenticated users (organization-scoped).
- Delete (DELETE): Superusers only.
 */
export type customersRetrieveResponse200 = {
  data: KippoCustomer
  status: 200
}

export type customersRetrieveResponseSuccess = (customersRetrieveResponse200) & {
  headers: Headers;
};
;

export type customersRetrieveResponse = (customersRetrieveResponseSuccess)

export const getCustomersRetrieveUrl = (id: string,) => {




  return `/api/customers/${id}/`
}

export const customersRetrieve = async (id: string, options?: RequestInit): Promise<customersRetrieveResponse> => {

  return customFetch<customersRetrieveResponse>(getCustomersRetrieveUrl(id),
  {
    ...options,
    method: 'GET'


  }
);}


/**
 * ViewSet for KippoCustomer model.

**Organization Scoping:**
- Regular users see only customers from organizations they belong to.
- Superusers see all customers.

**Filtering:**
- is_active: Filter by display_as_active (true/false).
- organization: UUID filter (still intersected with user's memberships).
- search: SearchFilter on `name`, `email`.

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users).
- Create (POST): Authenticated users (organization-membership enforced by serializer).
- Update (PUT/PATCH): Authenticated users (organization-scoped).
- Delete (DELETE): Superusers only.
 */
export type customersUpdateResponse200 = {
  data: KippoCustomer
  status: 200
}

export type customersUpdateResponseSuccess = (customersUpdateResponse200) & {
  headers: Headers;
};
;

export type customersUpdateResponse = (customersUpdateResponseSuccess)

export const getCustomersUpdateUrl = (id: string,) => {




  return `/api/customers/${id}/`
}

export const customersUpdate = async (id: string,
    kippoCustomerRequest: KippoCustomerRequest, options?: RequestInit): Promise<customersUpdateResponse> => {

  return customFetch<customersUpdateResponse>(getCustomersUpdateUrl(id),
  {
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      kippoCustomerRequest,)
  }
);}


/**
 * ViewSet for KippoCustomer model.

**Organization Scoping:**
- Regular users see only customers from organizations they belong to.
- Superusers see all customers.

**Filtering:**
- is_active: Filter by display_as_active (true/false).
- organization: UUID filter (still intersected with user's memberships).
- search: SearchFilter on `name`, `email`.

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users).
- Create (POST): Authenticated users (organization-membership enforced by serializer).
- Update (PUT/PATCH): Authenticated users (organization-scoped).
- Delete (DELETE): Superusers only.
 */
export type customersPartialUpdateResponse200 = {
  data: KippoCustomer
  status: 200
}

export type customersPartialUpdateResponseSuccess = (customersPartialUpdateResponse200) & {
  headers: Headers;
};
;

export type customersPartialUpdateResponse = (customersPartialUpdateResponseSuccess)

export const getCustomersPartialUpdateUrl = (id: string,) => {




  return `/api/customers/${id}/`
}

export const customersPartialUpdate = async (id: string,
    patchedKippoCustomerRequest: PatchedKippoCustomerRequest, options?: RequestInit): Promise<customersPartialUpdateResponse> => {

  return customFetch<customersPartialUpdateResponse>(getCustomersPartialUpdateUrl(id),
  {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      patchedKippoCustomerRequest,)
  }
);}


/**
 * ViewSet for KippoCustomer model.

**Organization Scoping:**
- Regular users see only customers from organizations they belong to.
- Superusers see all customers.

**Filtering:**
- is_active: Filter by display_as_active (true/false).
- organization: UUID filter (still intersected with user's memberships).
- search: SearchFilter on `name`, `email`.

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users).
- Create (POST): Authenticated users (organization-membership enforced by serializer).
- Update (PUT/PATCH): Authenticated users (organization-scoped).
- Delete (DELETE): Superusers only.
 */
export type customersDestroyResponse204 = {
  data: void
  status: 204
}

export type customersDestroyResponseSuccess = (customersDestroyResponse204) & {
  headers: Headers;
};
;

export type customersDestroyResponse = (customersDestroyResponseSuccess)

export const getCustomersDestroyUrl = (id: string,) => {




  return `/api/customers/${id}/`
}

export const customersDestroy = async (id: string, options?: RequestInit): Promise<customersDestroyResponse> => {

  return customFetch<customersDestroyResponse>(getCustomersDestroyUrl(id),
  {
    ...options,
    method: 'DELETE'


  }
);}


