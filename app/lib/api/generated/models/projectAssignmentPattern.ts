/**
 * Generated by orval v8.8.1 🍺
 * Do not edit manually.
 * Kippo Project Management API
 * REST API for managing Kippo projects, tasks, and effort tracking
 * OpenAPI spec version: 1.1.0
 */
import type { ProjectAssignmentPatternConflict } from './projectAssignmentPatternConflict';
import type { ProjectAssignmentPatternMember } from './projectAssignmentPatternMember';

/**
 * A complete suggested project-assignment pattern. Mirrors the Pydantic
`ProjectAssignmentPattern` shape from `projects.definitions`. Per kippo#231.

`pattern_ids` carries every strategy key that produced this pattern after dedup
(kippo#227 S3) — typically one entry, e.g. `['P1-max-reuse']`, but multiple when
strategies converged on the same members + monthly_percentages.
 */
export interface ProjectAssignmentPattern {
  /** Strategy keys that produced this pattern (one entry, or several after dedup). */
  pattern_ids: string[];
  /** Human-readable label derived from pattern_ids. */
  label: string;
  /**
     * Day-precision completion date; null when the pattern can't reach the allocated effort.
     * @nullable
     */
  estimated_completion: string | null;
  /** True when the pattern overshoots target_date or breaches the per-user 100% cap. */
  infeasible: boolean;
  conflicts: ProjectAssignmentPatternConflict[];
  members: ProjectAssignmentPatternMember[];
}
