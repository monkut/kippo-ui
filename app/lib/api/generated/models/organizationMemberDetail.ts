/**
 * Generated by orval v8.8.1 🍺
 * Do not edit manually.
 * Kippo Project Management API
 * REST API for managing Kippo projects, tasks, and effort tracking
 * OpenAPI spec version: 1.1.0
 */

/**
 * Org-scoped projection of `KippoUser` joined with their `OrganizationMembership`.

Returned by `GET /api/organizations/<id>/members/`. Adds the per-org PII
(`email`, `slack_*`) that `projects.serializers.OrganizationMemberSerializer`
intentionally omits — keep these two serializers separate so the per-project
contract consumed by kippo-ui#57 is not coupled to this richer shape. Per kippo#14.
 */
export interface OrganizationMemberDetail {
  /** KippoUser primary key. */
  user_id: string;
  username: string;
  /** Composed first + last + (github_login). */
  display_name: string;
  first_name: string;
  last_name: string;
  /** Per-org email from OrganizationMembership, not KippoUser. */
  email: string;
  github_login: string;
  is_developer: boolean;
  is_project_manager: boolean;
  slack_username: string;
  slack_user_id: string;
  slack_image_url: string;
  /**
     * Workdays this member is available in the calendar month requested via the `month` query parameter (committed weekdays minus public/personal holidays). Null/absent when `month` is not provided.
     * @nullable
     */
  available_work_days?: number | null;
}
