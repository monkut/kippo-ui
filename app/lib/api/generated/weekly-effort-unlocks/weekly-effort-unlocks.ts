/**
 * Generated by orval v8.8.1 🍺
 * Do not edit manually.
 * Kippo Project Management API
 * REST API for managing Kippo projects, tasks, and effort tracking
 * OpenAPI spec version: 1.1.0
 */
import type {
  PaginatedProjectWeeklyEffortUnlockList,
  ProjectWeeklyEffortUnlock,
  ProjectWeeklyEffortUnlockApproveRequestRequest,
  ProjectWeeklyEffortUnlockRequest,
  WeeklyEffortUnlocksListParams
} from '../models';

import { customFetch } from '../../custom-fetch';

/**
 * 週間稼働アンロックの申請・承認 API (kippo#33 / T18).

- **Create (POST)**: 認証ユーザが自分の締め後の週のアンロックを `organization` + `week_start` + `reason` で申請する
  (承認待ち。`user` は申請者本人に固定)。
- **approve (POST, detail)**: 組織admin (superuser または当該組織の `is_project_manager`) が承認し、
  再ロック期限 `expires_datetime` を設定する。承認されるとその期限まで当該週が編集可能になり、期限経過で自動再ロック。
- 自分のアンロック申請は自分で承認できない (staff が自分の週を勝手に開けられない原則を踏襲。superuser は除く)。

アンロックは申請ログでもあるため更新・削除は不可 (GET/POST のみ)。組織スコープ: 非superuserは所属組織の申請のみ閲覧可。
 */
export type weeklyEffortUnlocksListResponse200 = {
  data: PaginatedProjectWeeklyEffortUnlockList
  status: 200
}

export type weeklyEffortUnlocksListResponseSuccess = (weeklyEffortUnlocksListResponse200) & {
  headers: Headers;
};
;

export type weeklyEffortUnlocksListResponse = (weeklyEffortUnlocksListResponseSuccess)

export const getWeeklyEffortUnlocksListUrl = (params?: WeeklyEffortUnlocksListParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/weekly-effort-unlocks/?${stringifiedParams}` : `/api/weekly-effort-unlocks/`
}

export const weeklyEffortUnlocksList = async (params?: WeeklyEffortUnlocksListParams, options?: RequestInit): Promise<weeklyEffortUnlocksListResponse> => {

  return customFetch<weeklyEffortUnlocksListResponse>(getWeeklyEffortUnlocksListUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


/**
 * 週間稼働アンロックの申請・承認 API (kippo#33 / T18).

- **Create (POST)**: 認証ユーザが自分の締め後の週のアンロックを `organization` + `week_start` + `reason` で申請する
  (承認待ち。`user` は申請者本人に固定)。
- **approve (POST, detail)**: 組織admin (superuser または当該組織の `is_project_manager`) が承認し、
  再ロック期限 `expires_datetime` を設定する。承認されるとその期限まで当該週が編集可能になり、期限経過で自動再ロック。
- 自分のアンロック申請は自分で承認できない (staff が自分の週を勝手に開けられない原則を踏襲。superuser は除く)。

アンロックは申請ログでもあるため更新・削除は不可 (GET/POST のみ)。組織スコープ: 非superuserは所属組織の申請のみ閲覧可。
 */
export type weeklyEffortUnlocksCreateResponse201 = {
  data: ProjectWeeklyEffortUnlock
  status: 201
}

export type weeklyEffortUnlocksCreateResponseSuccess = (weeklyEffortUnlocksCreateResponse201) & {
  headers: Headers;
};
;

export type weeklyEffortUnlocksCreateResponse = (weeklyEffortUnlocksCreateResponseSuccess)

export const getWeeklyEffortUnlocksCreateUrl = () => {




  return `/api/weekly-effort-unlocks/`
}

export const weeklyEffortUnlocksCreate = async (projectWeeklyEffortUnlockRequest: ProjectWeeklyEffortUnlockRequest, options?: RequestInit): Promise<weeklyEffortUnlocksCreateResponse> => {

  return customFetch<weeklyEffortUnlocksCreateResponse>(getWeeklyEffortUnlocksCreateUrl(),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      projectWeeklyEffortUnlockRequest,)
  }
);}


/**
 * 週間稼働アンロックの申請・承認 API (kippo#33 / T18).

- **Create (POST)**: 認証ユーザが自分の締め後の週のアンロックを `organization` + `week_start` + `reason` で申請する
  (承認待ち。`user` は申請者本人に固定)。
- **approve (POST, detail)**: 組織admin (superuser または当該組織の `is_project_manager`) が承認し、
  再ロック期限 `expires_datetime` を設定する。承認されるとその期限まで当該週が編集可能になり、期限経過で自動再ロック。
- 自分のアンロック申請は自分で承認できない (staff が自分の週を勝手に開けられない原則を踏襲。superuser は除く)。

アンロックは申請ログでもあるため更新・削除は不可 (GET/POST のみ)。組織スコープ: 非superuserは所属組織の申請のみ閲覧可。
 */
export type weeklyEffortUnlocksRetrieveResponse200 = {
  data: ProjectWeeklyEffortUnlock
  status: 200
}

export type weeklyEffortUnlocksRetrieveResponseSuccess = (weeklyEffortUnlocksRetrieveResponse200) & {
  headers: Headers;
};
;

export type weeklyEffortUnlocksRetrieveResponse = (weeklyEffortUnlocksRetrieveResponseSuccess)

export const getWeeklyEffortUnlocksRetrieveUrl = (id: number,) => {




  return `/api/weekly-effort-unlocks/${id}/`
}

export const weeklyEffortUnlocksRetrieve = async (id: number, options?: RequestInit): Promise<weeklyEffortUnlocksRetrieveResponse> => {

  return customFetch<weeklyEffortUnlocksRetrieveResponse>(getWeeklyEffortUnlocksRetrieveUrl(id),
  {
    ...options,
    method: 'GET'


  }
);}


/**
 * アンロック申請を承認し、再ロック期限を設定する (組織admin限定)。
 */
export type weeklyEffortUnlocksApproveCreateResponse200 = {
  data: ProjectWeeklyEffortUnlock
  status: 200
}

export type weeklyEffortUnlocksApproveCreateResponseSuccess = (weeklyEffortUnlocksApproveCreateResponse200) & {
  headers: Headers;
};
;

export type weeklyEffortUnlocksApproveCreateResponse = (weeklyEffortUnlocksApproveCreateResponseSuccess)

export const getWeeklyEffortUnlocksApproveCreateUrl = (id: number,) => {




  return `/api/weekly-effort-unlocks/${id}/approve/`
}

export const weeklyEffortUnlocksApproveCreate = async (id: number,
    projectWeeklyEffortUnlockApproveRequestRequest: ProjectWeeklyEffortUnlockApproveRequestRequest, options?: RequestInit): Promise<weeklyEffortUnlocksApproveCreateResponse> => {

  return customFetch<weeklyEffortUnlocksApproveCreateResponse>(getWeeklyEffortUnlocksApproveCreateUrl(id),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      projectWeeklyEffortUnlockApproveRequestRequest,)
  }
);}


