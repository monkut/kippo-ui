/**
 * Generated by orval v8.8.1 🍺
 * Do not edit manually.
 * Kippo Project Management API
 * REST API for managing Kippo projects, tasks, and effort tracking
 * OpenAPI spec version: 1.1.0
 */
import type {
  KippoProjectOrganizationCategory,
  PaginatedKippoProjectOrganizationCategoryList,
  ProjectCategoriesListParams
} from '../models';

import { customFetch } from '../../custom-fetch';

/**
 * Read-only list of selectable project categories (kippo#43).

Backs the kippo-ui project create/edit form category picker.

**Organization Scoping:**
- Regular users see global default categories plus the categories of organizations they belong to.
- Superusers see all active categories.

**Filtering:**
- organization: UUID filter — narrows to that organization's categories (intersected with the
  user's memberships) while still including the global defaults.

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users).
 */
export type projectCategoriesListResponse200 = {
  data: PaginatedKippoProjectOrganizationCategoryList
  status: 200
}

export type projectCategoriesListResponseSuccess = (projectCategoriesListResponse200) & {
  headers: Headers;
};
;

export type projectCategoriesListResponse = (projectCategoriesListResponseSuccess)

export const getProjectCategoriesListUrl = (params?: ProjectCategoriesListParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/project-categories/?${stringifiedParams}` : `/api/project-categories/`
}

export const projectCategoriesList = async (params?: ProjectCategoriesListParams, options?: RequestInit): Promise<projectCategoriesListResponse> => {

  return customFetch<projectCategoriesListResponse>(getProjectCategoriesListUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


/**
 * Read-only list of selectable project categories (kippo#43).

Backs the kippo-ui project create/edit form category picker.

**Organization Scoping:**
- Regular users see global default categories plus the categories of organizations they belong to.
- Superusers see all active categories.

**Filtering:**
- organization: UUID filter — narrows to that organization's categories (intersected with the
  user's memberships) while still including the global defaults.

**Permissions:**
- Read (GET): Authenticated users (organization-scoped for regular users).
 */
export type projectCategoriesRetrieveResponse200 = {
  data: KippoProjectOrganizationCategory
  status: 200
}

export type projectCategoriesRetrieveResponseSuccess = (projectCategoriesRetrieveResponse200) & {
  headers: Headers;
};
;

export type projectCategoriesRetrieveResponse = (projectCategoriesRetrieveResponseSuccess)

export const getProjectCategoriesRetrieveUrl = (id: string,) => {




  return `/api/project-categories/${id}/`
}

export const projectCategoriesRetrieve = async (id: string, options?: RequestInit): Promise<projectCategoriesRetrieveResponse> => {

  return customFetch<projectCategoriesRetrieveResponse>(getProjectCategoriesRetrieveUrl(id),
  {
    ...options,
    method: 'GET'


  }
);}


